from util import *

## Initially I plan to come up with a flexible MLP solution that 
## accept a dynamic number of layers, but it seems that the autodiff
## takes forever to compile the code. To make it easier, I only 
## implemented two most common MLP here: MLP1(one hidden layer)
## MLP2 (two hidden layers)

